// Example program of how math will work in Kis
// Copied off of Bella Program Example including lots of math

set dozen = 12

meow(dozen % 3 ** 1)

kitty gcd(x: int,y: int):
    purr y == 0 ? x : gcd(y, x % y) 
    whisker dozen >= 3 || (gcd(1,10) != 5):
        dozen = dozen - 200 ** 1 ** 3
    nap 
nap
// NOTE:
- ** exponent
- % modulus
- E is scientific notation
- ternary operator (constrinat ? true : false)

